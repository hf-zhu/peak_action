name: Hello World
on: 
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'
jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
    - name: Run a single-line script
      run: >-
      curl 'https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=404da158-403b-431e-8be6-8d2470f63f5b' \
   -H 'Content-Type: application/json' \
   -d '
   {
        "msgtype": "text",
        "text": {
            "content": "Hi，同志们\n 今天的周报交了嘛！"
			"mentioned_list":["@all"],
        }
   }'
